[project]
name = "nautiq-ingestion"
version = "0.1.0"
description = "Módulo de Ingesta y WebSockets para Nautiq"
requires-python = ">=3.11.9"

dependencies = [
    "websockets>=16.0",
    "python-dotenv>=1.2.2",
    "confluent-kafka",
]
