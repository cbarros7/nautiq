# El frontal es JS: su ciclo de vida es npm, fuera del workspace uv.
node_modules/
dist/
tsconfig.tsbuildinfo

# Credenciales de build por entorno (.env.dev, .env.pro). La plantilla si se versiona.
.env
.env.*
!.env.example
