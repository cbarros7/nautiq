# Dockerfile para el Motor de Procesamiento (Apache Flink)
# Deberá contener:
# - Imagen base nativa de Apache Flink (ej. flink:1.17-java11 o versión compatible con PyFlink)
# - Compilación específica para arquitectura ARM (AArch64) si se despliega en OCI Ampere A1.
# - Dependencias adicionales necesarias.
