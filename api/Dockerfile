# Dockerfile para la Capa Cognitiva (FastAPI)
# Deberá contener:
# - Imagen base de Python ligera (ej. python:3.11-slim)
# - Instalación de dependencias (vía uv o pip)
# - Comando de arranque de Uvicorn (CMD ["uvicorn", "app.api.webhooks:app", "--host", "0.0.0.0", "--port", "8000"])
